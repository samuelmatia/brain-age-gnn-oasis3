name: Update Leaderboard

on:
  push:
    branches:
      - main
    paths:
      # CHANGE 1: Trigger when any .enc file is added to the submissions folder
      - 'submissions/*.enc'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas tabulate

      - name: Run Leaderboard Script
        env:
          TEST_LABELS: ${{ secrets.TEST_LABELS }}
        run: python update_leaderboard.py

      - name: Commit and Push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # CHANGE 2: Add EVERYTHING in submissions, leaderboard, and docs
          # This ensures the new .enc file AND the updated leaderboard files are tracked
          git add submissions/*.enc
          git add leaderboard/leaderboard.csv leaderboard/LEADERBOARD.md
          git add docs/leaderboard.html
          
          # 3. Only commit and push if there are changes
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-update leaderboard [skip ci]" && git push)